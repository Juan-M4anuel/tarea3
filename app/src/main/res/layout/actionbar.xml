<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar

    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#ffffff"
    android:elevation="@dimen/elevacion_actionbar"
    app:titleTextColor="#000000"
    android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    >

    <ImageView
    android:id="@+id/huellaperro"
    android:layout_width="40dp"
    android:layout_height="40dp"
    android:src="@mipmap/huellaperro"
    android:layout_gravity="left" />

    <TextView
        android:id="@+id/TituloApp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Petagram"
        android:textSize="22dp"
        android:textAlignment="center"
        android:layout_marginStart="50dp"
        android:layout_marginEnd="30dp"
        />
       <!-- <ImageButton
        android:id="@+id/estrella5"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@mipmap/estrella5"
        android:layout_marginHorizontal="0dp"
        />-->

   <!-- <item
        android:id="@+id/mRefresh"
        android:title="Refresh"
        android:orderInCategory="102"
        app:showAsAction="always"
        android:icon="@mipmap/estrella5"
    />-->


    >
</androidx.appcompat.widget.Toolbar>

<!--
    //este layout es solo para crear nuestra barra-->
